// config/db.js (sample)
const mysql = require('mysql2/promise');
module.exports = mysql.createPool({
  host:'localhost',
  user:'root',
  password:'',
  database:'demo',
  waitForConnections:true,
  connectionLimit:10
});